/*
 * The answers themselves. They are held in one place rather than in every file that reads them,
 * because they are sixteen kilobytes of data and nothing about them varies.
 */

#include "reciprocal-answers.h"
